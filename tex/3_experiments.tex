\chapter{Experiments}\label{experiments}
\section{Data pre-processing}\label{experiments_preProcessing}
% We would like to subdivide the reviews into smaller items on which to base our similarity evaluation. We also concentrate our study on the meaning of the sentences by removing all stop words. We considered as English stopwords those provided by the \texttt{nltk.corpus} package. For the same reason we lemmatized all tokens by using the method provided in the \texttt{spacy} module. Also, for each review we maintain each token once. In this way we codify each review through it's essential information.
\section{Jaccard similarity}